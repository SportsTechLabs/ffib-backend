spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/ffib
    username: ffib
    password: ffib

  datasource:
    url: jdbc:postgresql://localhost:5432/ffib
    username: ffib
    password: ffib

  flyway:
    enabled: false

  data:
    redis:
      host: localhost
      port: 6379